#Database
spring:
  datasource:
    url: jdbc:mysql://localhost/user_service_db?autoReconnect=true&useSSL=false&createDatabaseIfNotExist=true
    username: admin
    password: admin
  application:
    name: user-service

#Flyway
flyway:
  locations: db/migration

#Server
server:
  port: 8083
  context-path: /user-service

#Oauth2
oauth2:
  client-id: UserServer_PRZ_2017
  client-secret: xsw2#EDCCDE#2wsx
  check-token-url: http://localhost:8081/auth-server/oauth/check_token

#Resource server
resource-server:
  url: http://localhost:8082/project-service
  user-details:
    context-path: /user-details

#Mail service
mail-service:
  url: http://localhost:8085/mail-service
  mails:
    context-path: /internal-api/mails

#eureka
eureka:
  client:
    serviceurl:
      defaultzone: ${EUREKA_URI:http://localhost:8761/eureka}